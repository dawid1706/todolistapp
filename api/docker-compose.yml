version: "3.8"

services:
  api:
    build:
      context: ./api
    ports:
      - "3000:3000"
    env_file:
      - ./.env
    volumes:
      - ./api:/usr/src/app
      - /usr/src/app/node_modules
    command: npm start
